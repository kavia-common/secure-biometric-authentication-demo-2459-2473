<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      App theme wiring.
      We use a MaterialComponents theme to ensure BiometricPrompt and modern widgets
      have the correct styling on all supported Android versions.

      NOTE: This project uses classic Views/XML (no Compose).
    -->
    <style name="Theme.SecureBiometricDemo" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorSecondary">@color/secondary</item>

        <item name="android:statusBarColor">@color/primary_variant</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
